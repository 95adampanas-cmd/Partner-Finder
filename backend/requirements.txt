# OpenAI Agents SDK — wersje PRZYPIĘTE (zgodne ze sobą, jak lokalnie).
# Bez pinów Render instalował najnowsze, niezgodne wersje -> błąd cache_write_tokens.
openai-agents==0.17.7
openai==2.44.0

# Serwer web — używamy Starlette bezpośrednio (nie FastAPI)
starlette==1.3.1
uvicorn[standard]

# Narzędzia (tools)
requests
beautifulsoup4

# Konfiguracja i walidacja
python-dotenv
pydantic==2.13.4
